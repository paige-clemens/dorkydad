services:
  earring-maker:
    build: .
    ports:
      - "5000:5000"
    environment:
      - SECRET_KEY=change-me-in-production
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped

volumes:
  uploads:
